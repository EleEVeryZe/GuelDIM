import { Registro as RegistroInterface } from "../../interfaces/interfaces";

// Re-export the existing domain model from shared interface to avoid duplication.
export type Registro = RegistroInterface;
